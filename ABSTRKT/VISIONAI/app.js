const btn=document.querySelector('.menubtn'),links=document.querySelector('.links');if(btn)btn.addEventListener('click',()=>links.classList.toggle('open'));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(f);const amount=data.get('offerta')||'—';const company=data.get('azienda')||'Acquirente';const box=document.getElementById('result');if(box){box.innerHTML=`<div class="card" style="margin-top:18px"><h3>Offerta registrata nella demo</h3><p class="notice">Proponente: <b>${company}</b><br>Offerta economica: <b>€ ${amount}</b></p><p class="notice">Questa demo non invia dati a un server. Per la versione pubblica collegheremo il form a email/CRM.</p></div>`;box.scrollIntoView({behavior:'smooth'})}}));